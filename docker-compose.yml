services:
  app:
    build: .
    container_name: scala
    ports:
      - "8001:8001"
    env_file:
      - .env          # 👈 tell Docker to load .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
